{
  "name": "runner",
  "version": "1.0.0",
  "description": "Assistente de terminal multiplataforma para assinatura simulada de documentos.",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf cli/assinatura assinador/target assinador/build",
    "build": "npm run build:cli && npm run build:assinador",
    "build:cli": "cd cli && go build -o assinatura .",
    "build:assinador": "mvn -B package -f assinador/pom.xml",
    "start": "npm run build && echo '' && echo '>>> Abrindo menu (digite o número da opção)...' && ./cli/assinatura"
  },
  "engines": {
    "node": ">=18"
  }
}
